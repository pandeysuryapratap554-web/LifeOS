{
  "name": "lifeos",
  "version": "1.0.0",
  "description": "A personal productivity platform built with Node.js, Express, PostgreSQL, and vanilla JavaScript.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "check": "node --check backend/app.js && node --check backend/server.js && node --check backend/config/db.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
