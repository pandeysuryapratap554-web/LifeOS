# The port used by the LifeOS web server.
PORT=3000

# Use development locally and production after deployment.
NODE_ENV=development

# MySQL connection settings. Keep the real password only in .env, never in Git.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=lifeos
